# MaxKB backend refactored (FastAPI + SQLModel + Agno)
# Python >=3.11,<3.12
# Preferred install: `uv sync --extra dev` (see pyproject.toml).
# Pip fallback:       `pip install -r requirements.txt`
# Dependencies are intentionally unpinned here; pin via pyproject.toml for uv.

# --- Core ---
fastapi
uvicorn[standard]
sqlmodel
agno[openai,anthropic,groq,ollama,aws,dashscope,litellm,postgres,pgvector,memory]
alembic
asyncpg
arq
redis
pydantic-settings
pyjwt
python-multipart
pgvector

# --- Parsing / IO ---
jieba
pypdf
python-docx
openpyxl
beautifulsoup4
markdownify
charset-normalizer
polib
python-dotenv
httpx
psutil
bcrypt

# --- Dev (optional) ---
# pytest
# pytest-asyncio
# ruff

# --- Multimedia nodes (optional: image/tts/stt/video/ttv/mcp) ---
# mcp
# elevenlabs
# fal-client
# replicate
# luma
# moviepy
# opencv-python-headless
