# 2026-07-08

- 梳理了 MaxKB 后端技术栈（Django 5.2 + DRF + langchain/langgraph + PostgreSQL/pgvector + Redis + Celery，uv 包管理，ruff lint）。
- 通过 @command://init 创建了 `CODEBUDDY.md`（仓库根），含常用命令（uv 安装、migrate、runserver、celery、local_model、ruff、manage.py test、Docker 构建）与高层架构说明（多 app 单体、models_provider 模型抽象、RAG 数据流、chat_pipeline/flow 引擎、配置与多服务部署、扩展点）。
- 关键配置机制：`apps/maxkb/conf.py` 的 `ConfigManager` 读 `/opt/maxkb/conf/config.yml` 或 `MAXKB_CONFIG_TYPE=ENV` + `MAXKB_*` 环境变量；DB 引擎 `dj_db_conn_pool.backends.postgresql`，Redis 支持 Sentinel。
- 模型供应商抽象核心：`apps/models_provider/base_model_provider.py` 的 `IModelProvider` / `MaxKBBaseModel.new_instance` / `BaseModelCredential` / `ModelInfo`+`ModelInfoManage`，类型见 `ModelTypeConst`（LLM/EMBEDDING/STT/TTS/IMAGE/TTI/RERANKER/TTV/ITV），具体实现在 `impl/`。
