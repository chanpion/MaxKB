# 2026-07-09

## MaxKB backend/ FastAPI 重构（plan building 阶段）

完成 **阶段5：RAG+Custom Retriever** 剩余文件，全部在 `backend/app/rag/`：
- `splitter.py`：从 `apps/common/utils/split_model.py` 移植 `SplitModel`（纯 stdlib，无 jieba）。重点坑：原 Django 正则用普通字符串 `'(?<=\\n)...'`，移植为 raw string 时必须写成 `(?<=\n)`（单反斜杠），否则标题匹配失败、整篇被当成一块。已用样例验证分层分段正确。
- `parsers.py`：移植 `common/handle/impl/text/*_split_handle.py` → 字节转 markdown。支持 md/txt/pdf(pypdf 字号探测)/html(bs4+markdownify)/docx/python-docx/xlsx(openpyxl)/csv/zip。DOCX 图片暂跳过（需 OSS File 存储）。新增依赖：jieba, pypdf, python-docx, openpyxl, beautifulsoup4, markdownify, charset-normalizer（已写入 `backend/pyproject.toml`）。
- `embed.py`：移植 `normalize_for_embedding` / `chunk_text`(256字) / `sub_array` / `to_ts_vector`(jieba)，及 `embed_texts()` 经 `app.providers.get_embedder` 异步嵌入（threadpool）。
- `pipeline.py`：`ingest_document()` 编排 解析→拆分→嵌入→入库。Document/Paragraph 走 SQLModel async session，Embedding 走 raw asyncpg SQL（`::vector` + `to_tsvector('simple',...)`），与 `retriever.py` 一致；source_type 用 PARAGRAPH="1"；函数内 `session.commit()`。

注意：embedding 列固定 `vector(1536)`，故嵌入维度需 1536（OpenAI 默认）。
本地无网，`uv sync`/pytest/ruff/agno 导入均无法验证；拆分器已用 python3 本地验证。

下一步（阶段6）：Agno `Agent`+`Memory` 重写 chat_pipeline，SSE 流式（StreamingResponse + `astream_events`）。

### 阶段6 完成：`backend/app/agents/chat_agent.py`
- `AgnoRetriever`：Agno 兼容 retriever 适配器，包 `PgVectorRetriever`；`arun(query)` 用 `embed_texts` 算 query 向量 → `search()`，返回 `{content,title,similarity}`。
- `ChatAgent`：包 `agno.agent.Agent(model=get_llm(...), retriever=..., memory=..., instructions=...)`。
- `stream()`：`agent.arun(message, stream=True)` 异步生成器，逐块 yield SSE 帧 `data: {...}`。
- `build_memory()`：可选 `agno.memory.db.postgres.PostgresMemory`；不可用时降级为 None（无状态）。
- 注意：agno 未联网安装，方法签名（`arun(stream=True)` / `session_id`）需 `uv sync` 后实测校准；文件已 `py_compile` 通过。

剩余：阶段7 flow/Workflow、阶段8 tools/trigger、阶段9 测试+CI+API 路由（chat_agent 尚未接 API 端点）。

### 阶段7 完成：`backend/app/workflows/`（Agno Workflow 引擎）
- `compare.py`：从 `apps/application/flow/compare/` 移植全部 21 个比较器（eq/contain/regex/wildcard/gt/len_*/is_true…）+ `do_assertion`，纯 stdlib，无 Django。已本地验证。
- `state.py`：`WorkflowState` 共享上下文 + 引用解析（镜像 Django `reset_prompt`/`get_reference_field`）：`{NodeName.field}`、`global.field`、`chat.field` 占位符替换；`get_field`/`resolve_reference`/`do_assertion`。
- `nodes/base.py`：`StepNode` ABC + `NodeResult`（node_variable/workflow_variable/branch_id/is_result/chunks/status）。
- `nodes/`：最小节点集已实现 `start` / `ai-chat-node`(agno Agent+get_llm，懒导入 agno) / `search-knowledge-node`(PgVectorRetriever+embed_texts) / `tool-node`(懒加载 app.tools registry) / `condition-node` / `direct-reply-node` / `variable-assign-node`。`nodes/__init__.py` 为 `node_map` 注册表（待补齐约 140 个 step_node）。
- `engine.py`：`WorkflowEngine` 加载 MaxKB flow JSON（nodes/edges），迭代式图遍历，支持 condition 分支锚点匹配（`<node_id>_<branch>_right`）与 AND/OR fan-in（pending 队列重评估，修复 diamond join 漏执行 bug）；`run()` 返回答案 dict，`stream()` 是 SSE 异步生成器（node_start/answer/node_end/done 帧）。
- `sandbox.py`：受限执行边界——优先 RestrictedPython，回退 Docker 沙箱（stub），含超时/FSO 说明；供后续代码类节点复用。
- 测试 `app/tests/test_workflow_engine.py` 已写（pytest，依赖 uv sync 后运行）。

约束：本地无网，sqlalchemy/agno 未装，`import app.workflows`（经 rag.retriever→sqlalchemy）无法本地 import；仅 `compare.py`/`state.py` 逻辑已本地验证，其余依赖 `uv sync` 后校准。

下一步（阶段8）：`app/tools/` Agno tools 目录（Function 工具目录 + registry，`ToolNode` 已懒加载）+ `app/trigger/` 事件触发（从 celery-beat 扫描）。

### 阶段8 完成：`backend/app/tools/` + `backend/app/trigger/`
- `tools/base.py`：`BaseTool` ABC，`invoke(state, inputs)`（给 ToolNode 用）+ `to_agno()`（包装 `agno.tools.function.Function`，懒导入 agno）。
- `tools/registry.py`：`ToolRegistry`（id→tool）+ `get_agno_tools(tool_ids)` 给 Agent 用。
- `tools/builtin.py`：`CalculatorTool`/`CurrentTimeTool`（安全算术子集，eval 限内置为空）。
- `tools/code_tool.py`：`CodeTool` 执行 `tool.code`（约定定义 `execute(**kw)` 或 `result`），经 `workflows/sandbox.run_in_sandbox` 受限执行。
- `trigger/base.py`：`BaseTrigger` ABC（support/deploy/undeploy/execute）。
- `trigger/manager.py`：`TriggerManager` 从 `event_trigger`/`event_trigger_task` 表（asyncpg）读取并 dispatch，按 source_type 注册 handler（`register_handler`）。
- `trigger/scheduled.py`：`build_arq_cron(trigger_setting)` 把 legacy Celery-beat PeriodicTask 映射为 arq cron（`cron(...)` 懒导入 arq），`run_trigger` 任务入口。
- LLM 节点已接 `tool_ids` → `get_agno_tools`（agno 懒导入）。
- 本地验证：tools 测试 5 个全过（无需 agno）；workflow 引擎遍历/分支/diamond 已本地验证。

### 阶段9（收尾，进行中）
- 测试：`app/tests/test_workflow_engine.py`（需 uv sync 跑，因 import 链）、`app/tests/test_tools.py`（纯，已本地全过）。
- CI：`backend/.github/workflows/ci.yml` 已启用 `uv run pytest -q`（之前注释，已打开）；ruff + migration guard 已有。
- 更新 `CODEBUDDY.md`：新增 `backend/` FastAPI+SQLModel+Agno 服务章节（布局/命令/约束）。
- 关键 bug 已修：`NodeResult.branch_id` 必须是构造器关键字参数，不能塞进 `node_variable`（ConditionNode/ToolNode 异常分支/engine 异常分支都曾错放 → 条件分支失效）。已验证条件分支正确。

## 重构前端 Next.js（frontend/）缺失 API 层迁移
- 现象：`frontend/src` 启动报 `Module not found: @/lib/constants`，且整个 `@/lib/api/*` 目录在仓库中从未落地（git 未跟踪 frontend/）。
- 已创建 `frontend/src/lib/constants.ts`（THEME_KEY/LOCALE_KEY/TOKEN_KEY/TOKEN_COOKIE/LOGIN_PATH=/frontend/login/HOME_PATH=/frontend/home）。
- 已按旧版 `ui/src/api` + `ui/src/request` 契约从零重建 `@/lib/api` 层：
  - `lib/request/index.ts`：axios 封装，baseURL=`/admin/api`（next.config 已 rewrite 到后端 8080），请求头 `AUTHORIZATION: Bearer <token>`（token 取自 localStorage `maxkb-token`）、`Accept-Language`；响应拦截解析后端信封 `{code,data,message}`，`promise()` 直接 resolve `response.data`。
  - 模块：`api/login.ts`(具名导出 loginApi)、`api/knowledge/knowledge.ts`、`api/model/model.ts`、`api/model/provider.ts`、`api/workspace/folder.ts`、`api/shared-api.ts`(loadSharedApi 按 type 返回模块)、`api/type/{common,model}.ts`。
  - workspace 前缀：`/workspace/` + `useUserStore.getState().getWorkspaceId()`（替代旧版 pinia useStore）。
- `npx tsc --noEmit` 通过（exit 0）。注意：`systemShare/systemManage` 类 API 当前复用 workspace 实现，新前端尚未落地对应页面。
- 约定：新前端 basePath=`/frontend`，独立 Next 服务；后端沿用 Django `apps/`（/admin/api）。
