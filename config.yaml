# MaxKB 本地开发配置
# 启动时需设置环境变量 MAXKB_CONFIG=1，使 apps/maxkb/const.py 从本仓库根目录读取本文件
# （默认 root_path 为 /opt/maxkb/conf，本地不存在故报错）

# ===== 数据库 (PostgreSQL + pgvector) =====
# 注意：defaults 中无 DB_NAME，必须在此显式声明
DB_NAME: maxkb
DB_HOST: 10.58.12.60
DB_PORT: 5433
DB_USER: postgres
DB_PASSWORD: pgvector123
DB_ENGINE: dj_db_conn_pool.backends.postgresql
DB_MAX_OVERFLOW: 80

# ===== Redis =====
REDIS_HOST: 10.58.12.60
REDIS_PORT: 6379
# 本地 Redis 默认无密码，留空即可；若设了密码请填写
REDIS_PASSWORD: 12345
REDIS_DB: 0
REDIS_MAX_CONNECTIONS: 100

# ===== 其他 =====
LANGUAGE_CODE: zh-CN
DEBUG: True
